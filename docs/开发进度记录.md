# 开发进度记录

> 更新时间：2026-09-06（今天到此为止）

## 一、已完成（今天）

1. **面包板机器人从零搭起并跑通核心链路**
   - 硬件：ESP32-S3 N16R8（鹿小班 DevKitC-1 克隆板）+ INMP441 麦克风 + OLED 屏
   - 购买清单、接线表已整理成文档
2. **ESP-IDF 环境**：v5.5.5 安装成功
   - 解决了中文路径 GBK 编码报错 → 用英文路径 `C:\Users\phorh\Desktop\echocare\xiaozhi-esp32-main` 编译
   - 以后所有编译烧录都在英文路径下做
3. **固件编译 + 烧录成功**
   - `set-target esp32s3` → `menuconfig`（选 Bread Compact WiFi + SSD1306 128×64）→ `build` → `flash`
   - 串口：COM3
4. **联网 + 激活绑定成功**
   - 连的是手机热点 `vivo X300 Pro`
   - 激活码：`281579`，已在 xiaozhi.me 绑定
   - 状态机已进入 `idle`（待命），唤醒词「你好小智」引擎就绪
5. **代码改动（已推 GitHub，commit 498ac44）**
   - `application.cc`：激活码打印到串口日志（`===== ACTIVATION CODE: %s =====`）
   - 面包板购买清单、ESP-IDF 安装指南等文档

## 二、待办（明天继续）

### 屏幕不亮 —— 正在排查（最高优先级）
- 现状：屏幕初始化报错 `SET_MULTIPLEX failed` / `i2c transaction failed`，固件降级成"无屏模式"
- 已排除：地址 0x3C/0x3D、SDA/SCL 接反、上拉电阻（固件内部已有上拉）、屏幕型号（SSD1306）
- **已改代码（还没验证！）**：
  - `config.h`：`DISPLAY_SDA_PIN` 改为 `GPIO_NUM_8`，`DISPLAY_SCL_PIN` 改为 `GPIO_NUM_9`（原来 41/42，怀疑是 JTAG 引脚问题）
  - `compact_wifi_board.cc`：`.dev_addr` 恢复为 `0x3C`
- **明天第一步**：
  1. 把屏幕 SDA 改插 GPIO8、SCL 改插 GPIO9（VCC→3V3、GND→GND 不变）
  2. 重新 `build` + `flash` + `monitor`
  3. 看 `SSD1306 driver installed` 之后是否出现 `Turning display on`
  - 如果成功 → 是 GPIO41/42 的 JTAG 引脚问题，解决
  - 如果还是失败 → 屏幕模块本身可能是坏的，考虑换屏

### 喇叭功放未到货
- 到货后：MAX98357 功放 + 喇叭接线（DIN→GPIO7、BCLK→GPIO15、LRC→GPIO16）
- 到货后验证对话声音

## 三、重要备忘

- **编译烧录必须用英文路径**：`cd /d C:\Users\phorh\Desktop\echocare\xiaozhi-esp32-main`
- **编译命令**：`idf.py build` → `idf.py -p COM3 flash` → `idf.py -p COM3 monitor`
- **串口**：COM3
- **Git 推送**：本地 commit 后，在外部 PowerShell 用 `git push`（代理端口是 7897）
- **代码文件有两份**（内容同步）：
  - 中文路径（Git 仓库）：`C:\Users\phorh\Desktop\情感陪伴机器人\esp32-main\...`
  - 英文路径（编译用）：`C:\Users\phorh\Desktop\echocare\...`
  - 改代码时两边都要改，保持一致

## 四、代码改动清单（当前未提交的改动）

- `config.h`：DISPLAY_SDA_PIN → GPIO8，DISPLAY_SCL_PIN → GPIO9
- `compact_wifi_board.cc`：dev_addr = 0x3C（已从 0x3D 改回）

> 这两处改动等明天屏幕验证通过后再提交 Git。
